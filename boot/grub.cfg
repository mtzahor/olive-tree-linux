insmod part_gpt
insmod fat
insmod efi_gop

set gfxpayload=keep
set timeout=0
set default=0

menuentry "Olive Tree Linux" {
    search --file --set=root /bzImage
    linux /bzImage console=tty0 console=ttyS0,115200
    initrd /initramfs.cpio.gz
}
